export function getIconImageURL(name: string) {
  const prefixedName = name.startsWith('icn-') || name.startsWith('icon-') ? name : `icn-${name}`;
  return `https://static.toss.im/icons/png/4x/${prefixedName}.png`;
}
