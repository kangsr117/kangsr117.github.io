import { RenderTarget } from 'framer';

const staticRenderTargets: RenderTarget[] = [RenderTarget.thumbnail, RenderTarget.canvas, RenderTarget.export];

export function isStaticRenderTarget(): boolean {
  return staticRenderTargets.includes(RenderTarget.current());
}
