import { useRef } from 'react';
import generateID from '../../@tossteam/tds-web/react/utils/generateID';

export function useUID(prefix = '') {
  const uid = useRef(generateID(prefix));
  return uid.current;
}
