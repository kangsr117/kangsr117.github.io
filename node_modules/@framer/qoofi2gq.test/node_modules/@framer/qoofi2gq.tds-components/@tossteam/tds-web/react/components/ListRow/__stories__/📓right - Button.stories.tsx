import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { List, Button, ListRow } from 'src/react';

storiesOf('ListRow', module)
  .addDecorator(storyFn => <List>{storyFn()}</List>)
  .add('📓right - Button', () => (
    <>
      <ListRow
        contents="button"
        right={
          <Button type="primary" size="medium" style="fill" onClick={action('onClick')}>
            Button
          </Button>
        }
      />
      <ListRow
        contents="button"
        right={
          <Button type="primary" size="tiny" style="fill" onClick={action('onClick')}>
            Button
          </Button>
        }
      />
      <ListRow
        contents="button"
        right={
          <Button type="primary" size="tiny" style="weak" onClick={action('onClick')}>
            Button
          </Button>
        }
      />
      <ListRow
        contents="button"
        right={
          <Button type="dark" size="tiny" style="weak" onClick={action('onClick')}>
            Button
          </Button>
        }
      />
    </>
  ));
