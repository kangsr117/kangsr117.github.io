import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';
import { select, boolean } from '@storybook/addon-knobs';
import { adaptive } from '@tossteam/colors';

import { FixedBottomCTA, Button, Txt, TextButton } from 'src/react';

import Readme from '../README.md';
import Paragraphs from '../../BottomCTA/__stories__/Paragraphs';

storiesOf('FixedBottomCTA', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('TypeA: Basics', () => (
    <>
      <Paragraphs />
      <FixedBottomCTA
        onClick={action('onClick')}
        type={select('Type', ['primary', 'danger', 'dark', 'light'], 'primary')}
        loading={boolean('Loading', false)}
        background={select('Background', ['default', 'none'], 'default')}
        htmlType="submit"
      >
        CTA
      </FixedBottomCTA>
    </>
  ))
  .add('TypeA: with TopAccessory', () => (
    <FixedBottomCTA
      onClick={action('onClick')}
      type={select('Type', ['primary', 'danger', 'dark', 'light'], 'primary')}
      htmlType="submit"
      topAccessory="신청하기 버튼을 누름으로서 약관에 동의합니다."
    >
      신청하기
    </FixedBottomCTA>
  ))
  .add('TypeA: with TopButton', () => (
    <FixedBottomCTA
      onClick={action('onClick')}
      type={select('Type', ['primary', 'danger', 'dark', 'light'], 'primary')}
      htmlType="submit"
      topAccessory={<TextButton type="primary">취소하기</TextButton>}
    >
      신청하기
    </FixedBottomCTA>
  ))
  .add('TypeA: with BottomAccessory', () => (
    <FixedBottomCTA
      onClick={action('onClick')}
      type={select('Type', ['primary', 'danger', 'dark', 'light'], 'primary')}
      htmlType="submit"
      bottomAccessory={
        <Txt color={adaptive.grey900}>신청하기 버튼을 누름으로서 약관에 동의합니다.</Txt>
      }
    >
      신청하기
    </FixedBottomCTA>
  ))
  .add('TypeB', () => (
    <>
      <Paragraphs />
      <FixedBottomCTA.TypeB
        leftButton={<Button type="danger">Secondary</Button>}
        rightButton={<Button type="primary">Primary</Button>}
      />
    </>
  ))
  .add('TypeC', () => (
    <FixedBottomCTA.TypeC
      topText="토스 프라임 결제"
      bottomText="4,900 원"
      button={<Button>결제하기</Button>}
    />
  ));
