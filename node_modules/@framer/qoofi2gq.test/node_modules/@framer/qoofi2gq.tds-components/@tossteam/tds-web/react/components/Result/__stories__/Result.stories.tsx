import { action } from "@storybook/addon-actions";
import { storiesOf } from "@storybook/react";
import * as React from "react";
import Lottie from "src/react/storybook/Lottie";
import { Button } from "../../Button";
import { Icon } from "../../Icon";
import Result from "../Result";

storiesOf("Result", module)
  .add("With Icon", () => {
    return (
      <Result
        figure={<Icon name="icn-info-line" />}
        title="다시 접속해주세요"
        description={"페이지를 불러올 수 없습니다\n다시 시도해주세요"}
        button={<Button onClick={action("버튼클릭")}>재시도</Button>}
      />
    );
  })
  .add("With Lottie", () => {
    return (
      <Result
        figure={
          <Lottie
            url="https://static.toss.im/lotties/spot/spot-error.json"
            loop={false}
          />
        }
        title="서비스 점검중입니다"
        description={`이용에 불편을 드려 죄송합니다\n점검 후, 개선된 모습으로 돌아오겠습니다.`}
      />
    );
  });
