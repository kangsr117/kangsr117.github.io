import classNames from 'classnames';
import * as React from 'react';

import { List } from '../List';

interface Props {
  className?: string;
}

export const PostTable: React.FC<Props> = React.memo(({ className, children }) => {
  return (
    <List
      className={classNames(
        className,
        'post-table',
        'has-hairline-after',
        'has-hairline-after--padding24'
      )}
    >
      {children}
    </List>
  );
});
