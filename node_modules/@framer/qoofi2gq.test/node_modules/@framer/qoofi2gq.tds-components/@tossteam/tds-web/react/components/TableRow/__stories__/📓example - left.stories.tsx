import { storiesOf } from '@storybook/react';
import * as React from 'react';
import { text, number } from '@storybook/addon-knobs';

import { List, TableRowLeft } from 'src/react';

storiesOf('TableRow', module).add('📓example - left', () => {
  return (
    <>
      <h1>Default</h1>
      <List>
        <TableRowLeft left={text('left', '안녕하세요')} right={text('right', '감사합니다')} />
      </List>
      <h1>leftRatio: {number('leftRatio', 30)}</h1>
      <List>
        <TableRowLeft leftRatio={number('leftRatio', 30)} left="받는분" right="김토스" />
        <TableRowLeft
          leftRatio={number('leftRatio', 30)}
          left="입금 계좌"
          right="신한 ***29482820482"
        />
        <TableRowLeft leftRatio={number('leftRatio', 30)} left="받는 분 통장표시" right="치토스" />
      </List>
    </>
  );
});
