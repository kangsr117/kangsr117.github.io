import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { AmountTop } from 'src/react';

import Readme from '../README.md';

storiesOf('AmountTop', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('📓example', () => (
    <AmountTop
      title="32,287,763 원"
      subtitle={{ text: '수협 1263728193793', type: 'underline' }}
      topPadding={0}
      bottomPadding={0}
      rightButton={{
        text: '송금',
        onClick: action('onClick - AmountTop'),
        style: 'weak',
      }}
    />
  ));
