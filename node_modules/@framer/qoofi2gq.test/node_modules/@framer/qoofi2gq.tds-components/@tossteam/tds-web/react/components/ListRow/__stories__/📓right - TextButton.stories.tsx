import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { List, ListRow, TextButton } from 'src/react';

storiesOf('ListRow', module)
  .addDecorator(storyFn => <List>{storyFn()}</List>)
  .add('📓right - TextButton', () => (
    <>
      <ListRow
        contents="text-button"
        right={
          <TextButton type="primary" size="large" onClick={action('onClick')}>
            TextButton
          </TextButton>
        }
      />
      <ListRow
        contents="text-button"
        right={
          <TextButton type="primary" size="medium" onClick={action('onClick')}>
            TextButton
          </TextButton>
        }
      />
      <ListRow
        contents="text-button"
        right={
          <TextButton type="primary" size="small" onClick={action('onClick')}>
            TextButton
          </TextButton>
        }
      />
      <ListRow
        contents="text-button"
        right={
          <TextButton type="dark" size="large" onClick={action('onClick')}>
            TextButton
          </TextButton>
        }
      />
      <ListRow
        contents="text-button"
        right={
          <TextButton type="dark" size="medium" onClick={action('onClick')}>
            TextButton
          </TextButton>
        }
      />
      <ListRow
        contents="text-button"
        right={
          <TextButton type="dark" size="small" onClick={action('onClick')}>
            TextButton
          </TextButton>
        }
      />
    </>
  ));
