import * as React from "react";
import { ComponentProps, ReactNode } from "react";
import { Button } from "../Button";

interface Props
  extends Omit<ComponentProps<typeof Button>, "size" | "display"> {
  children: ReactNode;
}

function DialogCTAButton(props: Props) {
  return <Button display="block" size="large" {...props} />;
}

export default DialogCTAButton;
