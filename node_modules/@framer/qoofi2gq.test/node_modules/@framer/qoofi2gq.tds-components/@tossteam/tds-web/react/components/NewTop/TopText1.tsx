import * as React from "react";
import { ComponentProps } from "react";

import TopText from "./TopText";

type Props = Omit<ComponentProps<typeof TopText>, "typography" | "fontWeight">;

export default function TopText1({ className, ...rest }: Props) {
  return <TopText fontWeight="bold" typography="t1" {...rest} />;
}
