import { storiesOf } from '@storybook/react';
import * as React from 'react';
import { colors } from '@tossteam/colors';

import { List, ListRow, Icon } from 'src/react';

storiesOf('ListRow', module)
  .addDecorator(storyFn => <List>{storyFn()}</List>)
  .add('📓right - Icon', () => (
    <>
      <ListRow contents="guide-line" right={<Icon name="guide-line" color={colors.green500} />} />
      <ListRow contents="add" right={<Icon name="add" />} />
      <ListRow contents="bank-toss" right={<Icon name="bank-toss" />} />
      <ListRow contents="service-101-color" right={<Icon name="service-101-color" />} />
    </>
  ));
