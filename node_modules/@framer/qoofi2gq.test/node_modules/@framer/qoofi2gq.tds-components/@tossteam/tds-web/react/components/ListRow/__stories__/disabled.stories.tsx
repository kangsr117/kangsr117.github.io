import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { List, Button, Checkbox, FontWeight, ListRow, Switch, Text } from 'src/react';

storiesOf('ListRow', module)
  .addDecorator(storyFn => <List>{storyFn()}</List>)
  .add('disabled', () => (
    <>
      <ListRow
        disabled={true}
        left={<ListRow.Icon type="border" name="bank-nh" />}
        contents="disabled"
        right={
          <Text
            typography={ListRow.Typography.Medium}
            fontWeight={FontWeight.Regular}
            display="block"
          >
            Text
          </Text>
        }
      />
      <ListRow
        disabled={true}
        disabledStyle="type1"
        left={<ListRow.Icon type="border" name="bank-nh" />}
        contents="disabled"
        right={<Button size="tiny">Button</Button>}
      />
      <ListRow
        disabled={true}
        disabledStyle="type2"
        left={<ListRow.Icon type="border" name="bank-nh" />}
        contents="disabled"
        right={<Switch />}
      />
      <ListRow
        disabled={true}
        disabledStyle="type2"
        left={<ListRow.Icon type="border" name="bank-nh" />}
        contents="disabled"
        right={<Checkbox />}
      />
    </>
  ));
