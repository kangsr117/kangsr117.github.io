import { number } from '@storybook/addon-knobs';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { CardBackgrounds } from 'src/react';

import SampleListRow from '../../ListRow/__stories__/SampleListRow';
import Readme from '../README.md';

storiesOf('CardBackgrounds', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('📓example - full', () => {
    const radius = number('radius', 12);
    return (
      <>
        <CardBackgrounds>
          <CardBackgrounds.Item radius={radius}>
            <SampleListRow />
          </CardBackgrounds.Item>
          <CardBackgrounds.Item radius={radius}>
            <SampleListRow />
          </CardBackgrounds.Item>
          <CardBackgrounds.Item radius={radius}>
            <SampleListRow />
          </CardBackgrounds.Item>
        </CardBackgrounds>
      </>
    );
  });
