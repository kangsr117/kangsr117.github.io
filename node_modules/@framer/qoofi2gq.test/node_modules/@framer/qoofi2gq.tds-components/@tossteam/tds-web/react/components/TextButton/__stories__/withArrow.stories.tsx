import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { TextButton } from 'src/react';

storiesOf('TextButton', module)
  .addParameters({})
  .add('withArrow', () => {
    return (
      <>
        <TextButton size="small" withArrow={false}>
          withArrow: false
        </TextButton>
        <TextButton size="small" withArrow={true}>
          withArrow:true
        </TextButton>
      </>
    );
  });
