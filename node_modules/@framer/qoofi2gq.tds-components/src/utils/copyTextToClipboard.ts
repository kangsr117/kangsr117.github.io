import * as clipboard from 'clipboard-polyfill';

export async function copyTextToClipboard(text: string) {
  try {
    await clipboard.writeText(text);
  } catch {
    throw new Error('사용하시는 브라우저의 정책으로 인하여 복사 기능을 지원하지 않습니다.');
  }
}
