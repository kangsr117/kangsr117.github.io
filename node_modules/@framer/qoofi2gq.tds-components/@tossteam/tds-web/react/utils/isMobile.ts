const navigator = typeof window !== 'undefined' ? window.navigator : undefined;

export const isIOS = navigator?.userAgent.match(/iPhone/i) ?? false;
export const isAndroid = navigator?.userAgent.match(/Android/i) ?? false;
export const isMobile = isIOS || isAndroid;
