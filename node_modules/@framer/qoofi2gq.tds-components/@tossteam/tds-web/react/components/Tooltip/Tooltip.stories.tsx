import { number, select } from "@storybook/addon-knobs";
import { storiesOf } from "@storybook/react";
import * as React from "react";
import DummyList from "src/react/storybook/DummyList";
import { Button } from "../Button";
import Toast from "../Toast";
import Tooltip from "./Tooltip";

storiesOf("Tooltip", module)
  .add("Overview", () => {
    return (
      <div
        style={{
          display: "flex",
          flexDirection: "column",
          justifyContent: "center",
          alignItems: "center",
          height: "100%",
        }}
      >
        <Tooltip
          message="툴팁 메시지"
          placement={select(
            "Placement",
            ["auto", "right", "bottom", "left", "top"],
            "auto"
          )}
          offset={number("Offset", 10)}
        >
          <div>
            <Button>클릭해보세요</Button>
          </div>
        </Tooltip>
        <Toast
          open={true}
          icon="icn-attention-color"
          text="Tooltip의 children은 반드시 Block element여야 합니다."
        />
      </div>
    );
  })
  .add("onScroll", () => {
    return (
      <div>
        <DummyList />
        <Tooltip open={true} message="이 툴팁은 알아서 정렬됩니다">
          <div>
            <Button display="block">타겟</Button>
          </div>
        </Tooltip>
        <DummyList />
        <DummyList />
        <DummyList />
        <DummyList />
        <DummyList />
      </div>
    );
  });
