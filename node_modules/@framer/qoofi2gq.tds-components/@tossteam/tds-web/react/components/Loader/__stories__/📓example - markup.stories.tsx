import { storiesOf } from '@storybook/react';
import * as React from 'react';

import Readme from '../README.md';

storiesOf('Loader', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('📓example - markup.stories', () => (
    <>
      <blockquote>직접 마크업을 작성하는 환경에서는 아래와 같이 스크립트를 호출합니다.</blockquote>

      <pre>{`
<div className="loader loader--type-primary loader--size-large">
  <div className="loader__animation lottie" />
  <label className="loader__label">내역을 불러오고 있습니다.</label>
</div>

<script>
  lottieWeb.loadAnimation({
    renderer: 'svg',
    loop: true,
    autoplay: true,
    container: this.animationRef.current!,
    path: '//static.toss.im/tds/current/lottie/loader/loader-white.json',
    rendererSettings: {
      progressiveLoad: true,
      hideOnTransparent: true,
    },
  })
</script>
      `}</pre>
    </>
  ));
