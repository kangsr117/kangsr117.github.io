import { storiesOf } from '@storybook/react';
import * as React from 'react';

import TextButton from '../TextButton';

storiesOf('TextButton', module)
  .addParameters({})
  .add('as', () => {
    return (
      <>
        <TextButton as="a" href="https://google.com" size="small" withArrow={false}>
          Go to Google
        </TextButton>
      </>
    );
  });
