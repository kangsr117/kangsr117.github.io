import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { Error } from 'src/react';

import Readme from '../README.md';

storiesOf('Error', module)
  .addParameters({ readme: { sidebar: Readme } })
  .addDecorator(storyFn => (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        width: '100%',
      }}
    >
      {storyFn()}
    </div>
  ))
  .add('📓example - connect', () => (
    <Error
      iconName="info-line"
      title="다시 접속해주세요"
      description={`오랜 시간 사용하지 않아 보안을 위해\n접속을 종료하였습니다`}
      buttonText="나가기"
    />
  ));
