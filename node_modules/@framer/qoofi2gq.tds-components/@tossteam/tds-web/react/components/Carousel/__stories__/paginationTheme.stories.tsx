import { storiesOf } from '@storybook/react';
import * as React from 'react';

import Carousel from 'src/react/Carousel';

import Readme from '../README.md';
import SimpleSlide from './SimpleSlide';

storiesOf('Carousel', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('paginationTheme', () => {
    const paginationThemes: ['default', 'light', 'dark'] = ['default', 'light', 'dark'];
    const extraProps = { spaceBetween: 16 };

    return (
      <>
        {paginationThemes.map(paginationTheme => (
          <>
            <Carousel {...extraProps} paginationTheme={paginationTheme}>
              {[1, 2, 3, 4, 5].map(index => (
                <Carousel.Slide key={index}>
                  <SimpleSlide>paginationTheme = {paginationTheme}</SimpleSlide>
                </Carousel.Slide>
              ))}
            </Carousel>
            <br />
          </>
        ))}
      </>
    );
  });
