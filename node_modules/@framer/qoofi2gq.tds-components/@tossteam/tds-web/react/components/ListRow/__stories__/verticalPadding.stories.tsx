import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { List, ListRow } from 'src/react';

storiesOf('ListRow', module)
  .addDecorator(storyFn => <List>{storyFn()}</List>)
  .add('verticalPadding', () => (
    <>
      <ListRow verticalPadding="extraSmall" contents="extraSmall" />
      <ListRow verticalPadding="small" contents="small" />
      <ListRow verticalPadding="medium" contents="medium" />
      <ListRow verticalPadding="large" contents="large" />
    </>
  ));
