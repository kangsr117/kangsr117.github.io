import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { List, FontWeight, ListRow, Text } from 'src/react';

storiesOf('ListRow', module)
  .addDecorator(storyFn => <List>{storyFn()}</List>)
  .add('withArrow', () => (
    <ListRow
      withArrow={true}
      right={
        <Text
          typography={ListRow.Typography.Medium}
          fontWeight={FontWeight.Regular}
          ellipsisAfterLines={2}
        >
          withArrow
          <br />
          spacing default
        </Text>
      }
    />
  ));
