import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { List, Checkbox, ListRow } from 'src/react';

storiesOf('ListRow', module)
  .addDecorator(storyFn => <List>{storyFn()}</List>)
  .add('📓right - Checkbox', () => (
    <>
      <ListRow
        contents="checkbox"
        right={<Checkbox type="circle-small-primary" onChange={action('onChange')} />}
      />
      <ListRow
        contents="checkbox"
        right={<Checkbox type="primary" onChange={action('onChange')} />}
      />
      <ListRow contents="checkbox" right={<Checkbox type="star" onChange={action('onChange')} />} />
    </>
  ));
