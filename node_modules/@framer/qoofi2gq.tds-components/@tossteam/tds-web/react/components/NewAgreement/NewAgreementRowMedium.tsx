import classnames from "classnames";
import * as React from "react";
import { colors } from "../../../../colors";
import NewAgreementRow, {
  NewAgreementRowCheckboxProps,
  NewAgreementRowProps,
} from "./NewAgreementRow";

type Props = Omit<NewAgreementRowProps, "withBorder"> &
  NewAgreementRowCheckboxProps;

function NewAgreementRowMedium({ className, ...rest }: Props) {
  return (
    <NewAgreementRow
      className={classnames("m-new-agreement-row-medium", className)}
      typography="t6"
      fontWeight="medium"
      color={colors.grey700}
      {...rest}
    />
  );
}

export default NewAgreementRowMedium;
