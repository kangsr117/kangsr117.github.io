import { adaptive } from "../../../../../@tossteam/colors";
import * as React from "react";
import { ComponentProps } from "react";

import TopText from "./TopText";

type Props = Omit<ComponentProps<typeof TopText>, "typography" | "fontWeight">;

export default function TopText5({
  className,
  color = adaptive.grey700,
  ...rest
}: Props) {
  return (
    <TopText color={color} fontWeight="regular" typography="t5" {...rest} />
  );
}
