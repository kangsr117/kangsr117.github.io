import { storiesOf } from '@storybook/react';
import * as React from 'react';

import Readme from '../README.md';

storiesOf('Container', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('📓example', () => (
    <>
      <blockquote>
        현재 모든 story 에 Container 가 적용되어 있습니다. 사용법은 아래와 같습니다.
      </blockquote>
      <blockquote>
        Storybook 상단 toolbar 에서 viewport 를 iPad 로 변경해 보시면 차이를 확인할 수 있습니다.
      </blockquote>

      <pre>{`
<Container>{/* ... */}</Container>
  `}</pre>
    </>
  ));
