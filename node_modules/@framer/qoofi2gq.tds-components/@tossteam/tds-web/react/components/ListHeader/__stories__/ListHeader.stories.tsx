import { select } from "@storybook/addon-knobs";
import { storiesOf } from "@storybook/react";
import { colors } from "@tossteam/colors";
import * as React from "react";
import ListHeader from "../ListHeader";

storiesOf("ListHeader", module)
  .add("Row1", () => {
    const border = select("Border", ["full", "indented", "none"], "full");

    return (
      <>
        <ListHeader.Row1 border={border}>1RowTypeA</ListHeader.Row1>
        <ListHeader.Row1 type="B" border={border}>
          1RowTypeB
        </ListHeader.Row1>
        <ListHeader.Row1
          border={border}
          right={
            <ListHeader.RightText size="medium" withArrow={true}>
              Medium Right Text with Arrow
            </ListHeader.RightText>
          }
        >
          With Right Text
        </ListHeader.Row1>
        <ListHeader.Row1
          border={border}
          right={
            <ListHeader.RightText size="small">
              Small Right Text
            </ListHeader.RightText>
          }
        >
          With Right Text
        </ListHeader.Row1>
      </>
    );
  })
  .add("Row2", () => {
    const border = select("Border", ["full", "indented", "none"], "full");

    return (
      <>
        <ListHeader.Row2
          topText="ListHeader.Row2"
          bottomText="TypeA"
          border={border}
        />
        <ListHeader.Row2
          type="B"
          topText="ListHeader.Row2"
          bottomText="TypeB"
          border={border}
        />
        <ListHeader.Row2
          topText="Top Text Color: RED"
          topTextColor={colors.red500}
          bottomText="Bottom Text Color: BLUE"
          bottomTextColor={colors.blue500}
          border={border}
        />
      </>
    );
  });
