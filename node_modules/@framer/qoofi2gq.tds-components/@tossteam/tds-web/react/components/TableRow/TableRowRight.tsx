import * as React from 'react';
import cx from 'classnames';

import BaseTableRow from './BaseTableRow';

function TableRowRight({ className, ...rest }: React.ComponentProps<typeof BaseTableRow>) {
  return <BaseTableRow className={cx('table-row--right', className)} {...rest} />;
}

export default TableRowRight;
