import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { Icon } from 'src/react';

storiesOf('Icon', module).add('size', () => (
  <>
    <Icon name="thumb-up-line" size={48} />
    <Icon name="thumb-up-line" size={24} />
    <Icon name="thumb-up-line" size={12} />
  </>
));
