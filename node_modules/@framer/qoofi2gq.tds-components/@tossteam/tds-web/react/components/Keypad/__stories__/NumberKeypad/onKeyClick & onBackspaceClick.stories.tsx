import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { NumberKeypad } from 'src/react';

import Readme from '../../README.md';

storiesOf('Keypad/NumberKeypad', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('onKeyClick & onBackspaceClick', () => (
    <NumberKeypad
      onKeyClick={action('onKeyClick - NumberKeypad')}
      onBackspaceClick={action('onBackspaceClick - NumberKeypad')}
    />
  ));
