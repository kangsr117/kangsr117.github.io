import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { AlphabetKeypad } from 'src/react';

import Readme from '../../README.md';

storiesOf('Keypad/AlphabetKeypad', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('onKeyClick & onBackspaceClick', () => (
    <AlphabetKeypad
      onKeyClick={action('onKeyClick - AlphabetKeypad')}
      onBackspaceClick={action('onBackspaceClick - AlphabetKeypad')}
    />
  ));
