import cx from "classnames";
import * as React from "react";
import { ComponentProps } from "react";
import BottomCTA from "../../components/BottomCTA";

function BottomSheetCTA({
  className,
  takeSpace = true,
  ...props
}: Omit<ComponentProps<typeof BottomCTA.TypeA>, "fixed">) {
  return (
    <BottomCTA.TypeA
      className={cx("bottom-cta--fixed-in-bottom-sheet", className)}
      takeSpace={takeSpace}
      {...props}
    />
  );
}

BottomSheetCTA.TypeB = ({
  className,
  takeSpace = true,
  ...props
}: Omit<ComponentProps<typeof BottomCTA.TypeB>, "fixed">) => {
  return (
    <BottomCTA.TypeB
      className={cx("bottom-cta--fixed-in-bottom-sheet", className)}
      takeSpace={takeSpace}
      {...props}
    />
  );
};

BottomSheetCTA.TypeC = ({
  className,
  takeSpace = true,
  ...props
}: Omit<ComponentProps<typeof BottomCTA.TypeC>, "fixed">) => {
  return (
    <BottomCTA.TypeC
      className={cx("bottom-cta--fixed-in-bottom-sheet", className)}
      takeSpace={takeSpace}
      {...props}
    />
  );
};

export default BottomSheetCTA;
