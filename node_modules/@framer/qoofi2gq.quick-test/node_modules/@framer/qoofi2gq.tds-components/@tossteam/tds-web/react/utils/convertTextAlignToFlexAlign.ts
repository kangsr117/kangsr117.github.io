const alignPropertyMap = {
  left: 'flex-start',
  center: 'center',
  right: 'flex-end',
};

export default function(textAlign: 'left' | 'center' | 'right') {
  return alignPropertyMap[textAlign];
}
