import * as React from 'react';
import cx from 'classnames';

import BaseTableRow from './BaseTableRow';

function TableRowLeft({ className, ...rest }: React.ComponentProps<typeof BaseTableRow>) {
  return <BaseTableRow className={cx('table-row--left', className)} {...rest} />;
}

export default TableRowLeft;
