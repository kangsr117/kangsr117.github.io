import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { Form, TextArea } from 'src/react';

import Readme from '../README.md';

storiesOf('TextArea', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('📓example - full', () => {
    function TextAreaFullExample() {
      const [amount, setAmount] = React.useState('');

      function onChangeAmount(event: React.ChangeEvent<HTMLTextAreaElement>) {
        setAmount(event.target.value);
        action('onChange - TextArea - 레이블')(...arguments);
      }

      return (
        <Form>
          <Form.Group label="레이블">
            <TextArea placeholder="플레이스홀더" value={amount} onChange={onChangeAmount} />
          </Form.Group>
        </Form>
      );
    }

    return <TextAreaFullExample />;
  });
