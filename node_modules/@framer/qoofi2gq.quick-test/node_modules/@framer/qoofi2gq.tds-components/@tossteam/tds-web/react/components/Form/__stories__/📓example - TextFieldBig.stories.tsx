import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { Form, TextFieldBig } from 'src/react';

import Readme from '../README.md';

storiesOf('Form', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('📓example - TextFieldBig', () => (
    <Form inputComponent={TextFieldBig}>
      <Form.Group label="레이블" help="도움말이 필요할 경우 여기에 표시됩니다.">
        <TextFieldBig type="tel" placeholder="1,000" suffix="만원" name="money" />
      </Form.Group>
    </Form>
  ));
