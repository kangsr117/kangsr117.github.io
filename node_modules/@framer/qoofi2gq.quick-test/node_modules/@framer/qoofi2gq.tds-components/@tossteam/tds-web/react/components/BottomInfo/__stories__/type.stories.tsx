import { storiesOf } from '@storybook/react';
import * as React from 'react';
import { adaptive } from '@tossteam/colors';

import { BottomInfo, Text, Typography } from 'src/react';

import Readme from '../README.md';

storiesOf('BottomInfo', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('type', () => {
    return (
      <>
        <section>
          <h4>
            <code>type</code>이 <code>undefined</code>거나 <code>list</code>인 경우
          </h4>
          {[undefined, 'list'].map((type, index) => (
            <BottomInfo key={index} type={type as any}>
              <BottomInfo.Item>
                The standard Lorem Ipsum passage, used since the 1500s
              </BottomInfo.Item>
              <BottomInfo.Item>
                Section 1.10.32 of de Finibus Bonorum et Malorum, written by Cicero in 45 BC
              </BottomInfo.Item>
              <BottomInfo.Item>1914 translation by H. Rackham</BottomInfo.Item>
              <BottomInfo.Item>
                Section 1.10.33 of de Finibus Bonorum et Malorum, written by Cicero in 45 BC
              </BottomInfo.Item>
            </BottomInfo>
          ))}
        </section>
        <section>
          <h4>
            <code>type</code>이 <code>text</code>인 경우
          </h4>
          <BottomInfo type="text">
            <Text typography={Typography.T7} color={adaptive.grey700} stringToJSX display="block">
              (주)비바리퍼블리카
              <br />
              사업자등록번호: 120-88-01280 <br />
              대표: 이승건
            </Text>
          </BottomInfo>
        </section>
      </>
    );
  });
