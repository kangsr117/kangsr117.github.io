import { storiesOf } from '@storybook/react';
import * as React from 'react';
import { text, number } from '@storybook/addon-knobs';
import { colors } from '@tossteam/colors';

import { List, Txt } from 'src/react';

import TableRowRight from '../TableRowRight';

storiesOf('TableRow', module).add('📓example - right', () => {
  return (
    <>
      <h1>Default</h1>
      <List>
        <TableRowRight left={text('left', '안녕하세요')} right={text('right', '감사합니다')} />
        <TableRowRight left="받는분" right="김토스" />
        <TableRowRight left="입금 계좌" right="신한 ***29482820482" />
        <TableRowRight left="받는 분 통장표시" right="치토스" />
      </List>
      <h1>leftRatio: {number('leftRatio', 30)}</h1>
      <List>
        <TableRowRight leftRatio={number('leftRatio', 30)} left="받는분" right="김토스" />
        <TableRowRight
          leftRatio={number('leftRatio', 30)}
          left="입금 계좌"
          right="신한 ***29482820482"
        />
        <TableRowRight leftRatio={number('leftRatio', 30)} left="받는 분 통장표시" right="치토스" />
      </List>
      <h1>Composition</h1>
      <List>
        <TableRowRight
          left={<Txt color={colors.blue500}>받는 분</Txt>}
          right={
            <Txt color={colors.purple700} fontWeight="bold">
              김토스
            </Txt>
          }
        />
      </List>
    </>
  );
});
