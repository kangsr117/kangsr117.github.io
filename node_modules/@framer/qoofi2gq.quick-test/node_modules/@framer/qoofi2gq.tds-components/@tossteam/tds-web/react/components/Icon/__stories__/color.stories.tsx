import { storiesOf } from '@storybook/react';
import * as React from 'react';
import { colors, adaptive } from '@tossteam/colors';

import { Icon, Txt } from 'src/react';

storiesOf('Icon', module).add('color', () => (
  <>
    <blockquote>
      주의: <code>Icon</code> 의 <code>color</code> 는 <b>*-line</b> 만 변경 가능합니다
    </blockquote>
    <Txt display="block">Adaptive Colored Icon</Txt>
    <Icon name="heart-line" color={adaptive.grey500} />
    <br />
    <Txt display="block">Colored Icons</Txt>
    <Icon name="heart-line" color={colors.green500} />
    <Icon name="service-52-line" color={colors.red500} />
    <Icon name="dining-spoon-line" color={colors.teal500} />
    <Icon name="confirm-line" color={colors.yellow500} />
    <Icon name="thumb-up-line" color={colors.blue500} />
    <Icon name="navigation-ios-back" color={colors.blue700} />
    <Icon name="arrow-downwards" color={colors.red700} />
    <Icon name="arrow-downwards-line" color={colors.blue700} />
  </>
));
