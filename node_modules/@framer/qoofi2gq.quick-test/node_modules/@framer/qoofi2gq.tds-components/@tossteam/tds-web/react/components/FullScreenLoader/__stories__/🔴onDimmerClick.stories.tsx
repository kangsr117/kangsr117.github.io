import { action } from '@storybook/addon-actions';
import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { FullScreenLoader } from 'src/react';

import Readme from '../README.md';

storiesOf('Loader/FullScreenLoader', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('🔴onDimmerClick', () => (
    <FullScreenLoader
      label="내역을 불러오고 있습니다."
      onDimmerClick={action('onDimmerClick - FullScreenLoader')}
    />
  ));
