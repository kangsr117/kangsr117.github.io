import * as React from "react";
import { ReactNode } from "react";
import { adaptive } from "../../../../../@tossteam/colors";
import Txt from "../Text";

interface Props {
  children?: ReactNode;
}

function BottomSheetHeaderDescription({ children }: Props) {
  return (
    <Txt
      as="p"
      typography="t6"
      fontWeight="regular"
      color={adaptive.grey600}
      className="bottom-sheet__header-description"
    >
      {children}
    </Txt>
  );
}

export default BottomSheetHeaderDescription;
