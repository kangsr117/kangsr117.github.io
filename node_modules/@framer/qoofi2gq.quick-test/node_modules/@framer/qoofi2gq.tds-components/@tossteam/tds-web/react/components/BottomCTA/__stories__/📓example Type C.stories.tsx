import { storiesOf } from "@storybook/react";
import * as React from "react";
import { BottomCTA, BottomSheet, Container } from "src/react";
import { Button } from "../../Button";
import Readme from "../README.md";
import Paragraphs from "./Paragraphs";

storiesOf("BottomCTA", module)
  .addParameters({ readme: { sidebar: Readme } })
  .add("📓example - TypeC", () => (
    <BottomCTA.TypeC
      topText="토스 프라임 결제 토스 프라임 결제 토스 프라임 결제 토스 프라임 결제 토스 프라임 결제 토스 프라임 결제 토스 프라임 결제 "
      bottomText="4,900 원"
      button={<Button>결제하기</Button>}
    />
  ))
  .add("📓example - TypeC : as Fixed element ", () => (
    <Container>
      <Paragraphs />
      <BottomCTA.TypeC
        fixed={true}
        topText="토스 프라임 결제"
        bottomText="4,900 원"
        button={<Button>결제하기</Button>}
      />
    </Container>
  ))
  .add("📓example - TypeC : with BottomSheet", () => (
    <BottomSheet
      open={true}
      header={<BottomSheet.Header>계속하려면 확인하세요</BottomSheet.Header>}
    >
      <Paragraphs />
      <BottomSheet.CTA.TypeC
        topText="토스 프라임 결제"
        bottomText="4,900 원"
        button={<Button>결제하기</Button>}
      />
    </BottomSheet>
  ));
