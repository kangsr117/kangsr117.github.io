import classnames from "classnames";
import * as React from "react";
import NewAgreementRow, {
  NewAgreementRowCheckboxProps,
  NewAgreementRowProps,
} from "./NewAgreementRow";

type Props = NewAgreementRowProps & NewAgreementRowCheckboxProps;

function NewAgreementRowBig({ className, ...rest }: Props) {
  return (
    <NewAgreementRow
      className={classnames("m-new-agreement-row-big", className)}
      typography="st8"
      fontWeight="bold"
      {...rest}
    />
  );
}

export default NewAgreementRowBig;
