import { storiesOf } from '@storybook/react';
import _ from 'lodash';
import * as React from 'react';

import { Text } from 'src/react';

import Readme from '../README.md';

storiesOf('Text', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('ellipsisAfterLines', () => (
    <>
      <Text ellipsisAfterLines={1}>{_.times(1000, () => '1').join(' ')}</Text>
      <br />
      <Text ellipsisAfterLines={2}>{_.times(1000, () => '2').join(' ')}</Text>
      <br />
      <Text ellipsisAfterLines={3}>{_.times(1000, () => '3').join(' ')}</Text>
    </>
  ));
