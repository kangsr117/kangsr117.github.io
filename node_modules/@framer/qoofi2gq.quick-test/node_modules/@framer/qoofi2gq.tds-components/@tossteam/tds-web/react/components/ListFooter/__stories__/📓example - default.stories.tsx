import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { List, ListFooter01, ListHeader } from 'src/react';

import SampleListRow from '../../ListRow/__stories__/SampleListRow';
import Readme from '../README.md';

storiesOf('ListFooter', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('Default', () => (
    <List>
      <ListHeader>List Header</ListHeader>
      <SampleListRow />
      <SampleListRow />
      <ListFooter01>List Footer</ListFooter01>
    </List>
  ))
  .add('with Icon', () => (
    <List>
      <ListHeader>List Header</ListHeader>

      <SampleListRow />
      <SampleListRow />
      <ListFooter01 icon="add">청구서 보기</ListFooter01>
    </List>
  ));
