import { action } from "@storybook/addon-actions";
import { storiesOf } from "@storybook/react";
import * as React from "react";
import { SearchField, Txt } from "src/react";
import Readme from "../README.md";

storiesOf("SearchField", module)
  .addParameters({ readme: { sidebar: Readme } })
  .add("Basic", () => (
    <>
      <SearchField
        onChange={action("텍스트 업데이트")}
        placeholder="플레이스홀더"
      />
      <Txt>상단 고정이 아닌 블럭형 SearchField</Txt>
    </>
  ))
  .add("Fixed to top", () => (
    <>
      <SearchField
        onChange={action("텍스트 업데이트")}
        placeholder="fixed={true}를 주면 상단 고정이 됩니다"
        takeSpace
      />
      <Txt>일반 SearchField(상단 고정)</Txt>
    </>
  ));
