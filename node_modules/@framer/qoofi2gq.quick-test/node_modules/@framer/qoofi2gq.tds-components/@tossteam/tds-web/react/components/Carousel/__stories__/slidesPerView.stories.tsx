import { storiesOf } from '@storybook/react';
import * as React from 'react';

import Carousel from 'src/react/Carousel';

import Readme from '../README.md';
import SimpleSlide from './SimpleSlide';

storiesOf('Carousel', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('slidesPerView', () => {
    const slidesPerViews = [1, 1.2, 2, 2.3];
    const extraProps = { spaceBetween: 16 };

    return (
      <>
        {slidesPerViews.map(slidesPerView => (
          <>
            <Carousel {...extraProps} slidesPerView={slidesPerView}>
              {[1, 2, 3, 4, 5].map(index => (
                <Carousel.Slide key={index}>
                  <SimpleSlide>slidesPerView = {slidesPerView}</SimpleSlide>
                </Carousel.Slide>
              ))}
            </Carousel>
            <br />
          </>
        ))}
      </>
    );
  });
