import { storiesOf } from '@storybook/react';
import * as React from 'react';

import { Form, TextFieldLine } from 'src/react';

import Readme from '../README.md';

storiesOf('TextFieldLine', module)
  .addParameters({ readme: { sidebar: Readme } })
  .add('📓example - result', () => (
    <Form inputComponent={TextFieldLine}>
      <Form.Group>
        <TextFieldLine readOnly={true} label="금액" value="10,000 원" />
      </Form.Group>
      <Form.Group>
        <TextFieldLine readOnly={true} label="보낼사람" value="강수영" />
      </Form.Group>
      <Form.Group>
        <TextFieldLine readOnly={true} label="출금계좌" value="토스 신한금융투자 계좌" />
      </Form.Group>
      <Form.Group>
        <TextFieldLine
          readOnly={true}
          label="레이블"
          value="매월 30일"
          subText="종료일 2018년 11월 20일"
        />
      </Form.Group>
    </Form>
  ));
